# 安全策略

## 报告漏洞

如果你发现安全漏洞，**请不要在公开 Issue 中报告**。

请发送邮件至 **1406152161@qq.com**，我会在 48 小时内回复。

## 安全实践

### API 密钥

- 所有 AI Provider 的 API Key 通过环境变量注入，**不允许**硬编码在代码中
- `.env` 文件已在 `.gitignore` 中，不会被提交到仓库
- 使用 `.env.example` 作为模板，不包含真实密钥

### 文件沙箱

- 每个角色运行时受 `FileScope` 限制，只能写入分配给他的目录
- 跨角色的文件访问需要通过 `Workspace` 接口

### 依赖安全

- Dependabot 自动监控依赖更新
- CI 包含 `pip-audit` 检查已知漏洞

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 0.1.x | ✅ 活跃开发 |

## 披露政策

- 漏洞确认后 7 天内发布修复
- 重大漏洞将通过 GitHub Security Advisory 披露
